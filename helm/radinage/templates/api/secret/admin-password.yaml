{{- $app := .Values.apps.api }}
{{- if and $app.enabled $app.adminPasswordSecret.create }}
{{- $ctx := dict "appName" "api" "appValues" $app "root" $ }}
{{- $secretName := include "radinage.adminPasswordSecretName" $ }}
{{- $secretKey := $app.adminPasswordSecret.key }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "radinage.labels" $ctx | nindent 4 }}
    {{- with $app.adminPasswordSecret.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with $app.adminPasswordSecret.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
data:
  {{- if and $existing (hasKey $existing.data $secretKey) }}
  {{ $secretKey }}: {{ index $existing.data $secretKey }}
  {{- else }}
  {{ $secretKey }}: {{ randAlphaNum 32 | b64enc }}
  {{- end }}
{{- end }}
